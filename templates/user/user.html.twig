{% extends "base.html.twig" %}

{% block body %}
    <body>
    {{ form_start(form) }}
    {{ form_row(form.username) }}
    {{ form_row(form.email) }}
    {{ form_row(form.password.first) }}
    {{ form_row(form.password.second) }}
    {{ form_row(form.roles) }}
    <button type="submit">Save</button>
    <a href="{{ path('delete_user', {'id' : user.id}) }}">Delete My Account</a>
    {{ form_end(form) }}
    </body>
{% endblock %}
